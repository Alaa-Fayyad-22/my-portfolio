@model MyPortfolio.Models.ContactInfo

<footer class="site-footer mt-auto py-7">
    <div class="container">
        <div class="row align-items-center">
            <!-- Left: Contact Info -->
            <div class="col-md-6 mb-4 mb-md-0 text-center text-md-start">
                <h5 class="text-gold mb-3">📬 Contact</h5>
                <ul class="list-unstyled small">
                    @if (!string.IsNullOrEmpty(Model.Email))
                    {
                        <li class="mb-2"><i class="bi bi-envelope-fill me-2 text-gold"></i><a class="text-light" href="mailto:@Model.Email">@Model.Email</a></li>
                    }
                    @if (!string.IsNullOrEmpty(Model.Phone))
                    {
                        <li class="mb-2" ><i class="bi bi-telephone-fill me-2 text-gold"></i><a class="text-light" href="tel:@Model.Phone">@Model.Phone</a></li>
                    }
                    @if (!string.IsNullOrEmpty(Model.Whatsapp))
                    {
                        <li class="mb-2" ><i class="bi bi-whatsapp me-2 text-gold"></i><a class="text-light" target="_blank" href="https://api.whatsapp.com/send/?phone=@Model.Whatsapp&text=&type=phone_number&app_absent=0">WhatsApp</a></li>
                    }
                    @if (!string.IsNullOrEmpty(Model.Address))
                    {
                        <li class="mb-2"><i class="bi bi-geo-alt-fill me-2 text-gold"></i>@Model.Address</li>
                    }
                </ul>
            </div>

            <!-- Right: Social Links -->
            <div class="col-md-6 text-center text-md-end">
                <h5 class="text-gold mb-3">🔗 Follow Me</h5>
                <div class="d-flex justify-content-center justify-content-md-end gap-3 fs-5">
                    @if (!string.IsNullOrEmpty(Model.LinkedIn))
                    {
                        <a href="https://www.linkedin.com/in/@Model.LinkedIn" target="_blank" class="text-gold footer-icon"><i class="bi bi-linkedin"></i></a>
                    }
                    @if (!string.IsNullOrEmpty(Model.GitHub))
                    {
                        <a href="@Model.GitHub" target="_blank" class="text-gold footer-icon"><i class="bi bi-github"></i></a>
                    }
                    @if (!string.IsNullOrEmpty(Model.Instagram))
                    {
                        <a href="https://www.instagram.com/@Model.Instagram" target="_blank" class="text-gold footer-icon"><i class="bi bi-instagram"></i></a>
                    }
                    @if (!string.IsNullOrEmpty(Model.Facebook))
                    {
                        <a href="@Model.Facebook" target="_blank" class="text-gold footer-icon"><i class="bi bi-facebook"></i></a>
                    }
                </div>
            </div>
        </div>

        <hr class="border-light my-4" />

        <p class="text-center small mb-0">© @DateTime.Now.Year Alaa Fayyad. All rights reserved.</p>
    </div>
</footer>
